# P4B SD01 Style Freeze Result

Status: `STYLE_VISUAL_GATE_PASS / CANDIDATE_A_FROZEN / EXACT_HEAD_CI_PASS / READY_FOR_RELEASE`

Packet: `VC-P4B-SD01-STYLE-FREEZE-001`

Base: `main @ 0f4964bd0719768c1828e402b5e20aef3dfdfc46`

## Human visual Gate result

The owner reviewed the read-only A/B comparison page using the same VERIFIED SC01 F Cutout and approved **Candidate A**.

Frozen P4B decision:

- selected background: `#171B20` — Gallery Surface;
- Candidate B `#0E1114`: not selected for SD01 v1;
- pure black `#000000`: rejected;
- no additional visual treatment is authorized for v1.

Visual rationale recorded from the review:

- `#171B20` preserves stronger separation around dark wood regions, fine branches and cavities than the deeper `#0E1114` shell background;
- it remains within the canonical DRIFT CURIO dark-gallery token system while behaving as a dedicated media/display surface;
- pure black suppresses low-luminance wood detail and is incompatible with the intended premium Gallery presentation.

## Frozen SD01 v1 production contract

`VERIFIED_SC01_ARCHIVE -> exact #171B20 background -> same dimensions -> opaque RGB PNG -> no resize/crop -> no relight -> no synthetic shadow -> no vignette -> no generative inference`.

Additional invariants:

- preserve exact piece silhouette;
- preserve source wood RGB under alpha compositing; only the background fill is introduced;
- no tonal curve, color grading, sharpening, denoise, perspective change or geometry mutation;
- browser may submit an archive asset ID only; no arbitrary D/F path or destination path;
- production implementation must continue to use server-side verified P3 archive + Manifest provenance as source truth.

## Source-backed facts retained

- `SD01 = Static Dark Master` / Dark Master.
- Formal filename family: `{SKU}__dark__master__wf-SD01__vNNN.png`.
- Canonical dark gallery tokens:
  - gallery background `#0E1114`;
  - gallery surface `#171B20`;
  - gallery raised `#20252B`;
  - gallery border `#2D333A`.
- Brand direction: charcoal/dark background + controlled lighting; prioritize natural silhouette, texture and sculptural form.

## Read-only review surface

`/sd01-style.html` rendered one verified SC01 F Cutout in three visual references:

1. Candidate A — Gallery Surface `#171B20` — **approved**;
2. Candidate B — Gallery Background `#0E1114` — not selected;
3. Reject reference — pure black `#000000` — rejected.

The page remains intentionally read-only and does not call derivative, QA or archive mutation APIs.

Windows launcher:

`tools/P4B_SD01_STYLE_REVIEW.ps1`

The launcher only synchronizes the review branch, safely restarts the local Visual Console runtime and opens the read-only comparison page. It writes no production asset.

## Automated validation

The P4B branch CI contract includes:

- parse all P3/P4/P4B Windows scripts, including `P4B_SD01_STYLE_REVIEW.ps1`;
- parse archive/SW01/SD01 review JavaScript;
- `npm ci`;
- full tests;
- full build.

Latest P4B release-candidate CI: PASS. Exact head and matching run are recorded in PR #7 metadata and GitHub Actions.

## Release boundary

P4B freezes style only. The released style-freeze branch still does **not** enable SD01 production execution:

- `SD01` remains `NOT_REGISTERED`;
- `executable=false`;
- no D Dark staging asset is generated by P4B;
- no derivative/QA journal mutation is enabled for SD01;
- no Manifest `destinations.dark` archive is enabled by P4B;
- no Registry production promotion is allowed until P4C implementation validation passes.

## Next Gate

P4C SD01 implementation must use the frozen Candidate A contract and prove deterministic compositing, source provenance, no-overwrite/versioning, QA, Gate15 `destinations.dark`, restart reconstruction, idempotence and one real Windows physical asset before release.
