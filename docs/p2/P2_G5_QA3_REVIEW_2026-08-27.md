# Visual Console P2 — G5 QA-3 Review

Date: 2026-08-27
Packet: `VC-P2-SC01-CONTROL-LOOP-001`
G4B: `APPROVED`
QA class: `QA-3`
Status: `G5_REQUEST_CHANGES / S7_REPAIR_COMPLETE / G4B_REPAIR_REVIEW_REQUIRED`

## Independence model

The P2 implementation owner is `GPT-5.6 Sol + connected GitHub connector`, so implementation-session reasoning is not counted as independent QA evidence. This G5 review uses:

1. Human Owner evidence from the target Windows + real ComfyUI runtime;
2. GitHub Actions deterministic CI;
3. repository code/diff inspection against the approved G4A/Packet.

G5 does not self-approve. Final G5 Gate remains a Human Owner decision after the bounded repair review and focused QA re-check.

## Audited target and retained evidence

- repository: `wuge988/visual-console`
- base: `main @ 024da283e9f92e35c1b0460f02df0eaa4a6ad877`
- accepted production/UI review HEAD: `133c4fdca4dc40c60a2bc33b6cfac773132eb1dd`
- production/UI CI: `#116 success`
- final S7 repair code HEAD: `c6962cf3e5d8efe4155a3ba33945646702ccf20e`
- final S7 repair CI: `#125 success`
- PR #2 remains Draft / Open / Unmerged.

Independent Human Owner runtime evidence retained as valid:

- six application routes work;
- Core API 4177 / P2 Control 4179 / Web 5173 start together;
- ComfyUI offline/online status is truthful;
- real SC01 API JSON registers and Workflow count changes 0 → 1;
- one real RAW produces `__wf-SC01__v001.png` in D staging;
- Queue displays a real prompt_id;
- dynamic QA is usable after bounded UI/zoom repair;
- three additional RAW assets complete serially as `v002/v003/v004` without OOM/overwrite;
- Visual Console restart restores SC01 registration, jobs, assets and QA state;
- F RAW remains available and P2 performs no Gate-15 archive;
- final Workspace structure is accepted/frozen by the Human Owner.

## Code/security PASS areas retained

- P2 service listens only on `127.0.0.1:4179` and mutating/admin routes enforce loopback request origin.
- ComfyUI URL is constrained to `127.0.0.1 / localhost / ::1`; browser cannot provide arbitrary host URLs.
- RAW sources are resolved via registered `site_id + item_id + asset_id`, not browser filesystem paths.
- F RAW input preparation uses verified copy without deleting the source.
- prompt result capture is correlated to `prompt_id`, not newest-file guessing.
- output path traversal is rejected; generated content is revalidated under staging root before serving.
- SC01 staging versions use no-overwrite allocation and `wx` file creation.
- different workflow hashes cannot silently replace an existing SC01 binding.
- journal torn-tail behavior preserves prior valid snapshots.
- Gate-15-equivalent archive states/actions remain absent.

## G5-P1-01 — Repaired, focused re-check pending

Original finding: frozen SC01 model/class was not fully enforced.

S7 repair now:

- requires exactly one supported RMBG class node;
- accepts the two known production representations `RMBG` and `Remove Background (RMBG)`;
- requires exact `inputs.model === "RMBG-2.0"`;
- keeps the frozen numeric/boolean signature and accepted Alpha/background_color behavior;
- adds negative regression tests for unsupported class mimicry and model drift.

Status: `REPAIRED / G5_FOCUSED_RECHECK_REQUIRED`.

## G5-P1-02 — Repaired, focused re-check pending

Original finding: persisted `CAPTURED` could be stranded across restart.

S7 repair now:

- reconstructs complete `CAPTURED` metadata directly as `QA_PENDING`;
- persists that recovered state back to `jobs.jsonl` when the journal is intact;
- lets existing torn-tail repair rewrite recovered states when the journal tail is torn;
- converts incomplete generated metadata to persisted `FAILED_CAPTURE / CAPTURED_RECOVERY_METADATA_INCOMPLETE`;
- does not enqueue recovered `CAPTURED`, so no ComfyUI rerun or duplicate staging version occurs;
- regression test verifies persisted recovery and retained filename/version.

Status: `REPAIRED / G5_FOCUSED_RECHECK_REQUIRED`.

## Automated repair evidence

Final repair code HEAD `c6962cf3e5d8efe4155a3ba33945646702ccf20e` passed GitHub Actions CI #125:

- `npm ci` — success
- `npm test` — success
- `npm run build` — success

Production/test repair was confined to `apps/server/src/p2-runtime.ts` and `apps/server/test/p2-runtime.test.ts`; later commits are evidence/status documentation only.

## Non-blocking residual risks

The following remain bounded P2 risks and do not independently block G5 after the focused re-check:

- generated ComfyUI input derivatives are not automatically garbage-collected in P2;
- P2 control plane remains a separate loopback-only 4179 process instead of being consolidated into 4177;
- SC01 capture fails closed when prompt history exposes multiple output PNGs;
- archive/approved-asset lifecycle is intentionally absent until the separate Gate-15-equivalent slice.

## Current recommendation and hard stop

Current status remains `G5_REQUEST_CHANGES` until the repaired slice passes bounded G4B repair review and independent G5 focused re-check.

Next hard stop: `G4B_REPAIR_REVIEW_REQUIRED`.

No archive, Merge, Deployment, new feature work, or UI redesign is authorized.