# Visual Console P2 — G5 QA-3 Review

Date: 2026-08-27
Packet: `VC-P2-SC01-CONTROL-LOOP-001`
G4B: `APPROVED`
QA class: `QA-3`
Status: `G5_QA_REVIEW_COMPLETE / S7_REPAIR_COMPLETE / FOCUSED_RECHECK_PASS_RECOMMENDED / HUMAN_G5_DECISION_REQUIRED`

## Independence model

The P2 implementation owner is `GPT-5.6 Sol + connected GitHub connector`, so implementation-session reasoning is not counted as independent QA evidence. This G5 review uses:

1. Human Owner evidence from the target Windows + real ComfyUI runtime;
2. GitHub Actions deterministic CI;
3. exact repository code/diff/test evidence against the approved G4A/Packet.

Final G5 approval remains a Human Owner decision.

## Original retained evidence

The first G5 review confirmed the already validated P2 runtime path and security boundaries, including six routes, real SC01 registration/execution, prompt-correlated output capture, serial `v001-v004`, dynamic QA, restart reconstruction, F RAW preservation, localhost-only control plane, loopback-only ComfyUI, path containment, no-overwrite staging versions, journal torn-tail handling, and absence of Gate-15 archive.

That review returned two blocking findings only:

- `G5-P1-01` — SC01 model/class guardrail incomplete;
- `G5-P1-02` — persisted `CAPTURED` restart gap.

## Bounded repair approvals

Human Owner approved:

`批准P2进入S7有界修复，仅修复G5-P1-01和G5-P1-02`

After repair, Human Owner approved the bounded repair review:

`G4B-P2修复复核通过，进入G5 focused QA-3复检`

The exact G4A binding remained unchanged.

## Focused re-check outcome

Detailed focused evidence is recorded in:

`docs/p2/P2_G5_QA3_FOCUSED_RECHECK_2026-08-27.md`

### G5-P1-01 — CLOSED / PASS

The validator now requires exactly one supported RMBG class representation (`RMBG` or `Remove Background (RMBG)`), exact `inputs.model === "RMBG-2.0"`, and the frozen numeric/boolean signature. Negative tests reject an unrelated lookalike class and model drift while preserving the accepted Alpha + `background_color` case.

### G5-P1-02 — CLOSED / PASS

`readJournal()` now recovers a complete persisted `CAPTURED` snapshot directly to `QA_PENDING`, persists the recovered snapshot, and fails incomplete metadata closed as `FAILED_CAPTURE / CAPTURED_RECOVERY_METADATA_INCOMPLETE`. The recovery path does not enqueue the job and therefore does not resubmit ComfyUI, recapture output, or allocate another staging version. Focused tests verify the state is persisted to disk and retains the existing generated filename/version.

## Authoritative automated evidence

Final repair code HEAD:

`c6962cf3e5d8efe4155a3ba33945646702ccf20e`

GitHub Actions CI #125:

- `npm ci` — success
- `npm test` — success
- `npm run build` — success

Repository comparison after `c6962cf...` found documentation-only successors and no later production/test code drift before the focused re-check record.

## Non-blocking residual risks

The following remain bounded P2 risks and do not block G5:

- generated ComfyUI input derivatives are not automatically garbage-collected in P2;
- P2 control plane remains a separate loopback-only 4179 process instead of being consolidated into 4177;
- SC01 capture fails closed when prompt history exposes multiple output PNGs;
- archive/approved-asset lifecycle is intentionally absent until the separate Gate-15-equivalent slice.

## G5 recommendation

Current recommendation:

`G5_PASS_RECOMMENDED / HUMAN_G5_DECISION_REQUIRED`

Both original blocking findings are closed. Do not treat this recommendation as self-approval by the implementation owner. If the Human Owner approves G5, proceed to the next JZ-v0.4 stage while still withholding Merge/Deployment Release Decision and the separate Gate-15-equivalent archive authorization.