# Visual Console P2 — S7 Bounded Repair for G5 Findings

Date: 2026-08-27
Workflow: `JZ-v0.4 / MODE_A_STANDARD_FRONTEND`
Packet: `VC-P2-SC01-CONTROL-LOOP-001`
Status: `S7_REPAIR_COMPLETE / AUTOMATED_VALIDATION_PASS / G4B_REPAIR_REVIEW_REQUIRED`

## Authorization

Human Owner explicitly approved:

`批准P2进入S7有界修复，仅修复G5-P1-01和G5-P1-02`

The existing exact G4A binding remains valid. No binding field changed.

## Repair scope

Production/test repair is confined to:

- `apps/server/src/p2-runtime.ts`
- `apps/server/test/p2-runtime.test.ts`

The additional changes after the repair are evidence/status documentation only. No UI, archive, site storage, workflow registry metadata, deployment, merge, or other workflow execution code changed.

## G5-P1-01 — Closed by repair

Finding: SC01 registration validated the numeric/boolean signature but did not require the expected RMBG class representation or exact `RMBG-2.0` model.

Repair:

- require exactly one supported RMBG class node;
- supported class representations are `RMBG` and `Remove Background (RMBG)`;
- require exact `inputs.model === "RMBG-2.0"`;
- preserve exact frozen sensitivity/process/mask/boolean checks;
- preserve correct `background = Alpha` handling while ignoring independent `background_color` metadata;
- negative tests reject unsupported lookalike class and model drift;
- both known valid RMBG class representations are covered by positive regression tests.

## G5-P1-02 — Closed by repair

Finding: a crash after persisted `CAPTURED` but before persisted `QA_PENDING` could strand a completed generated asset.

Repair:

- journal reconstruction recognizes latest `CAPTURED` snapshots;
- a snapshot with complete deterministic generated metadata is promoted directly to `QA_PENDING`;
- incomplete captured metadata fails closed as `FAILED_CAPTURE` with `CAPTURED_RECOVERY_METADATA_INCOMPLETE`;
- recovered state is appended back to `jobs.jsonl` when the journal is intact;
- when a torn tail is present, `readJournal()` returns the recovered state to the existing torn-journal repair path, which rewrites the valid recovered snapshots;
- recovery does not enqueue the job and therefore does not resubmit ComfyUI, recapture output, or allocate another staging version;
- regression tests verify the recovered state is actually persisted, retains the existing filename/version, and incomplete metadata is persisted as capture failure.

## Automated validation

Final repair code HEAD:

`c6962cf3e5d8efe4155a3ba33945646702ccf20e`

GitHub Actions CI #125:

- `npm ci` — success
- `npm test` — success
- `npm run build` — success

Earlier intermediate repair CI #122 also passed, but #125 is the authoritative final repair-code run because it includes persisted recovery snapshots.

## Non-scope confirmation

Still not implemented or authorized:

- Gate-15-equivalent archive to F approved roots;
- staging deletion after archive;
- generated-asset Trash/Restore;
- any non-SC01 workflow execution;
- SC01 parameter retuning or 1536;
- HEIC normalization;
- WAN/video generation;
- public/cloud exposure;
- auth/multi-user/multi-GPU;
- legacy cleanup;
- Merge;
- Deployment.

## Next hard stop

`G4B_REPAIR_REVIEW_REQUIRED`

After bounded repair review, G5 QA-3 must perform a focused independent re-check of the two repaired findings. G5 is not self-approved by the implementer.