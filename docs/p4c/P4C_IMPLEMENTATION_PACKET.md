# Visual Console P4C — SD01 Static Dark Master Implementation Packet

Date: 2026-08-27
Packet: `VC-P4C-SD01-DARK-001`
Base: `main @ 34868242085499c36821d131c611d1823a5865cd`
Branch: `feat/p4c-sd01-production`
Mode: `MODE_A_STANDARD_FRONTEND`
Status: `IMPLEMENTATION_COMPLETE / TARGET_WINDOWS_GATE_PASS / RELEASE_READY`

## Frozen visual contract

P4B human review approved Candidate A and P4B is released on main.

`VERIFIED_SC01_ARCHIVE -> exact #171B20 background -> same dimensions -> opaque RGB PNG -> no resize/crop -> no relight -> no synthetic shadow -> no vignette -> no generative inference`.

No additional visual treatment is authorized in SD01 v1.

## Source truth

The browser may submit only the source SC01 archive asset ID. A valid source must be proven by both the latest durable P3 `ARCHIVE_SNAPSHOT` in `archives.jsonl` and a matching Manifest Gate15 `VERIFIED_ARCHIVE` history row. It must be the same Site/SKU, `workflow_code=SC01`, `destination_key=cutout`, use the standardized filename, remain inside formal `asset_root`, and its current F bytes must still match persisted SHA256 and byte size.

No browser-supplied D/F path, output path, destination path, filename, SHA or size is trusted.

## Renderer

Renderer ID: `sd01-flat-gallery-surface-rgb-v1`.

Background: exact `#171B20` = RGB `(23,27,32)`. Input is the released fail-closed 8-bit non-interlaced RGBA PNG contract. Each source pixel is alpha-composited over the frozen background. Output is deterministic opaque RGB PNG with identical width and height.

## D staging and journal

Staging:

`<staging_root>/visual-console/<SKU>/dark/{SKU}__dark__master__wf-SD01__vNNN.png`

Rules: `v001..v999`, never overwrite, serialized generation, durable version allocation from `dark-derivatives.jsonl` plus current standardized D files. Generation writes `GENERATING` first, rereads generated bytes, requires exact renderer byte equality, then persists `QA_PENDING`. Recovery only promotes a torn `GENERATING` record if verified source + deterministic rerender + existing D bytes agree exactly; otherwise `FAILED_GENERATION`.

## QA

Individual PASS / FAIL / NOTE. QA mutations require the D file to still match persisted SHA256/size. An already archived Dark Master cannot be reclassified.

## Gate15 dark archive

Only `SD01 QA_PASS` may archive. Destination comes only from `Manifest.destinations.dark` under formal `asset_root`.

Safety order:

1. server-side derivative identity;
2. verify D SHA256 + size;
3. resolve/validate `destinations.dark`;
4. F create/no-overwrite;
5. verify F SHA256 + size;
6. atomically append exactly one matching Manifest Gate15 history row;
7. re-verify F;
8. delete D last;
9. append shared `ARCHIVE_SNAPSHOT`.

Retry after durable history is idempotent. Same-name/different-content or conflicting history fails closed.

## API

Local-only:

- `GET /api/dark-derivatives?site_id=...&item_id=...`
- `POST /api/dark-derivatives/SD01/batch`
- `GET /api/dark-derivatives/qa?site_id=...&item_id=...`
- `POST /api/dark-derivatives/qa/:assetId/decision`
- `POST /api/dark-derivatives/archive/:siteId/:itemId/:assetId`
- `GET /api/dark-derivatives/assets/:siteId/:itemId/:assetId/content`

## Release truth

Real Windows evidence passed on runtime-tested implementation head `13ff8ab453b3a7d479d920b41a25cafa30ea90a4`:

- `P4C_SD01_FINAL_PHYSICAL_SELF_CHECK=PASS`
- `P4C_SD01_WINDOWS_GATE=PASS`
- formal asset `c756a0e4657ba8b9923625b2156c67cd`
- evidence `E:\AI_PROJECTS\DRIFT_CURIO_VISUAL\visual-console-p2\drift-curio\evidence\P4C_SD01_20260827-214807`

Registry is now promoted to `VALIDATED_LOCAL_RENDERER / executable=true`. The accepted six-page shell integrates SD01 without adding a permanent navigation page.

All commits after the physical Gate are release-only UI/Registry/test/docs changes; the renderer, backend QA/archive semantics, physical self-checks and D/F mutation contract remain identical to the runtime-tested implementation.

## Automated evidence

Final release packaging head passes full CI:

`Parse Windows physical self-checks → Parse validation JavaScript → npm ci → npm test → npm run build`.

## Non-scope

No scene/video workflow, relighting, shadow synthesis, vignette, generative model, RAW deletion, public deployment, `control_root` rename, 4179→4177 consolidation, staging GC, or weakening of P3/P4A contracts.
